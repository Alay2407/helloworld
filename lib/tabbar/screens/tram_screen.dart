import 'package:flutter/material.dart';
class TramScreen extends StatelessWidget {
  const TramScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.green,
      body: Center(
        child: Text('Tram Screen'),
      ),
    );
  }

}
